<%= form_with(model: animal) do |form| %>
  <% if animal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

      <ul>
        <% animal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :farmer_id, style: "display: block" %>
    <%# <%= form.text_field :farmer_id %>
    <%= form.collection_radio_buttons :farmer_id, @farmers, :id, :name %>
    <%= form.collection_select :farmer_id, @farmers, :id, :name %>
  </div>

  <div>
    <%= form.submit %>    
  </div>
<% end %>
